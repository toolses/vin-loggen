# ── VinLoggen API – required environment variables ────────────────────────────
# Copy to .env (root) or set in Render dashboard → Environment.

# Supabase PostgreSQL direct connection
# Find under: Supabase Dashboard → Settings → Database → Connection string (URI)
SUPABASE_CONNECTION_STRING="postgresql://postgres:[PASSWORD]@db.quvkgyiybiyfwnjpembr.supabase.co:5432/postgres"

# Google AI Studio – Gemini 2.0 Flash
GEMINI_API_KEY="your-google-ai-studio-api-key"

# Optional: comma-separated extra origins for CORS (e.g. custom Vercel domain)
# CORS_ALLOWED_ORIGINS="https://vinloggen.no,https://www.vinloggen.no"

# Optional: comma-separated Supabase user UUIDs to grant admin access
# ADMIN_USER_IDS="a1b2c3d4-e5f6-7890-abcd-ef1234567890"
