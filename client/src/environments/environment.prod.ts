export const environment = {
  production: true,
  supabaseUrl: '',       // Injected via Azure Static Web Apps application settings
  supabaseAnonKey: '',   // Injected via Azure Static Web Apps application settings
  apiBaseUrl: '/api',
} as const;
